READ ME file

To run both scripts the user is asked at first to insert the name that the user chose for the folder of wordpress which is located inside the htdocs folder of Xampp.
Then it is asked to log in as an admin by inserting username and password.

Note: for the XSS injection, since selenium is used, the user must install the geckodriver for Mozilla Firefox in the folder where python file is stored.